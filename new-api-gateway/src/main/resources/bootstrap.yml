server:
  port: 9900

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${nasos.server-addr:127.0.0.1:8848}
        namespace: 
        group: DEFAULT_GROUP
        username: nacos
        password: nacos
  application:
    name: @artifactId@         